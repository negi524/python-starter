[tool.poetry]
name = "python-starter"
version = "0.1.0"
description = "Template for Python project"
authors = ["negi524 <egtnrumpbiizarre.info@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.scripts]
dev = "src.python_starter.main:main"

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.28.2"
PyYAML = "^6.0"
black = "^23.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
